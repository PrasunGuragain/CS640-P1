Topology:
# Sender (port 5000) ---- Emulator0 (port 3000) ---- Emulator1 (port 3001) ---- Requester (port 4000)

On the emulator, no delay or packet lost; queue size is large enough to avoid queue full error.