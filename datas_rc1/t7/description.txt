Topology:
    # Sender (port 5000) ---- Emulator1 (port 3000) ---- Requester (port 4000)
    # Sender (port 5001) -----|

set the delay for the link between emulator and requester as 1000 (same as timeout), 
